

//Importing the update state action from the types foilder
import {UPDATE_STATE} from "../types/types"


const update_person =  {
    type:UPDATE_STATE,
    payload: "Tobi Ilori"
}

export default update_person