This folder contains a notes.txt file which explains the basics of MongoDB , these asics include basic CRUD operations , using the mongo Shell , using mongo in command line and also using MongoDB's compass application