package Menu_Example;

public interface Iterator {
	//Indicates whether there are more elements to iterate over
			public boolean hasNext();
	//Returns the next elemenmt
			public Object next();
}
